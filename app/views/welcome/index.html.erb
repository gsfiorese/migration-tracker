<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Migration Tracker - Welcome</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

<!-- Header with logo, navigation, search, and buttons -->
<header class="p-2 text-bg-dark">
  <div class="container">
    <div class="d-flex align-items-center justify-content-between">
      <a href="/" class="d-flex align-items-center text-white text-decoration-none">
        <%= image_tag "Migration.png", alt: "Migration Tracker Logo", class: "me-2 logo" %>
      </a>
      <div class="text-end">
        <%= link_to "Login", new_user_session_path, class: "btn btn-outline-light me-2" %>
        <%= link_to "Sign-up", new_user_registration_path, class: "btn btn-warning" %>
      </div>
    </div>
  </div>
</header>


  <!-- Tabs Section -->
  <div class="container my-4">
    <ul class="nav nav-tabs" id="myTab" role="tablist">
      <% ["Australia", "New South Wales", "Victoria", "Queensland", "South Australia", "Western Australia", "Tasmania"].each do |state| %>
        <li class="nav-item" role="presentation">
          <a
            class="nav-link <%= "active" if state.parameterize == "australia" %>"
            id="<%= state.parameterize %>-tab"
            href="#"
            data-tab="<%= state.parameterize %>">
            <%= state %>
          </a>
        </li>
      <% end %>
    </ul>

    <div class="tab-content" id="myTabContent">
      <div class="loading-message text-center mt-4">Select a tab to view data.</div>
      <div class="data-container mt-4"></div>
    </div>
  </div>

  <!-- Bootstrap JS Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
